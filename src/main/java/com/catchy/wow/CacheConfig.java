package com.catchy.wow;

/**
 * Placeholder left intentionally non-config to avoid conflicting
 * bean definitions with the application's primary `CacheConfig`.
 *
 * The active cache configuration for the wow feature is provided
 * by `WowCacheConfig` in the same package.
 */
public class CacheConfig {
    // intentionally left empty to avoid duplicate bean registration
}
